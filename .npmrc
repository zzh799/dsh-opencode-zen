# 本仓库把多代 DSH 的真实测试包放进同一棵依赖树（根 devDependencies + tests/hosts/*），
# 它们的 peer 范围互相冲突（如 cordis ^4.0.3 与 ~4.0.4、dsh-* 的 alpha 版本会浮动到新版），
# 严格 peer 解析必然 ERESOLVE，只能宽松解析。
# pnpm 从 git 安装本插件时，会在临时目录执行裸 `npm install` + `npm run prepack` 构建 lib/，
# 该路径不会携带任何命令行参数，只有仓库自带这份配置才能安装成功。
legacy-peer-deps=true
